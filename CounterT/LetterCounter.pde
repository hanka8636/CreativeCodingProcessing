/*class LetterCounter{
LetterCounter(){}

void countInDict(){

}

int countInVerse(String[] words){
    
        int s = 1;
    float x = 20;
  float y = 64;
  
    for (int j =0; j< words.length; j++)
    {
      String xval = words[j];
      for (int k = 0; k<keys.length; k++)
      if (xval.equals(keys[k])){
              String ka = keys[k];
        int r =  occurance.get(keys[k]);
        println("val " + r);
       s = r;
      textSize(s*12);
       score +=s;
       
       println(words[j]);
      println(s);
      text(words[j], x, y);
        // Move along the x-axis
        if( x<width)
      x += textWidth(words[j] + " "); 
      else 
      x=20;
   }
      //<>//
      
      
    }
     y += 16;
     return s;
    }
    
    int countWor
}
*/
